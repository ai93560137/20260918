# 2026-09-20 改動清單與上線順序

分支 `claude/tradingview-pine-script-cor9fw`，最新 commit `204056a`。
驗證狀態：`tests/test_risk_gate.py` **51/51 通過**；main.py 語法 OK；
兩個 HTML 標籤配對 OK；Pine v4 無 CE10156 風險；MQ5 括號配對 OK。

---

## 一、這次改了什麼（一句話）

**三級共振被量度後完全退場；電閘改由五道風控關卡驅動；進場換成錦囊 v4
（M15 區間突破 · 只做多 · 波動門檻 · 抱 10 根 · 無價格停損）；
EA 拆掉結構性負期望的 TP/SL 改定時出場，回撤線由 90% 收到 20%。**

> ⚠️ 本文件最後更新於錦囊 v4 進 GCP 之後（commit `97d775b`）。
> 舊版寫的「錦囊第一次通過異常值檢驗」**已作廢** —— 那來自一個有 bug 的
> Python 實作。正確的全期數字見第六節。

---

## 二、要部署的五個檔案

| # | 檔案 | 去哪裡 | 為什麼 |
|---|---|---|---|
| 1 | `main.py` | GCP Cloud Function | 電閘改風控驅動、只做多、五關、錦囊 v4 進場 |
| 2 | `jinnang_sheet.html` | 與 main.py 同目錄 | 執行單加波動關卡，數字重算 |
| 3 | `jinnang_tracker.html` | 與 main.py 同目錄 | 進度表節奏改 1.9 年，數字重算 |
| 4 | `tradingview/zhugeliang_jinnang_v4.pine` | TradingView | 只做多＋ATR ≥ 0.10% |
| 5 | `mt5/RiskManager_HKD_Zhuge_V23.mq5` | MetaEditor | 定時出場、回撤 20%、只做多 |
| 6 | `gates.html` | 與 main.py 同目錄 | 顯示五道風控關卡；標示哪些開關已不被讀取 |
| 7 | `order.html` | 與 main.py 同目錄 | 預覽跟著實際引擎走（不送 TP） |

> ⚠️ **送單參數頁的 `account_type` 要改成 `demo`。** 目前預設是 `real`，
> 不改的話單會進實盤戶口。
>
> ⚠️ **`gates.html` 與 `order.html` 也要一起換。** 舊版會顯示兩件假訊息：
> ① 送單預覽照舊引擎算（止損倍數不對、還多一個不會送出的 TP 欄位）；
> ② 六個開關（趨勢確認／布林結構／M1 收線／RSI／平倉冷卻／加碼間距）
> 在錦囊底下根本不會被讀到，但舊版看起來像是生效中。

### ⚠️ 關掉 TradingView 的快訊

GCP 現在自己跑錦囊 v4。**兩邊都開會在同一個訊號下兩張單（2 盎司 = 3.35x 槓桿）**，
而且 TradingView 那條路不經過風控五關 —— 沒有曝險上限、沒有單日虧損、
沒有訓練節奏、沒有新聞鎖。

**保留 GCP、關掉 TradingView 快訊。** 但要知道差別：

| | TradingView 快訊 | GCP |
|---|---|---|
| 風控五關 | ❌ 無 | ✅ 有 |
| 依賴 EA | ❌ 不依賴 | ⚠️ **完全依賴**（M15 資料由 EA 心跳送上來） |
| 開始判定 | 立即 | **要累積 100 根已收盤 M15 ≈ 25 小時** |

⚠️ **EA 停了，GCP 就瞎了。** TradingView 那條路是獨立的，GCP 不是。
如果 MT5 關機或 EA 卸載，GCP 不會有任何訊號，而且**不會報錯** ——
儀表板會停在「【累積中⏳】」或最後一次判定。每天看一眼。

### ⚠️ 部署後要等 25 小時

GCP 的錦囊引擎要累積 **100 根已收盤 M15**（`JN_MIN_BARS`）才開始判定。
儀表板會顯示「【累積中⏳】」。**星期一部署後不會馬上有訊號，那是正常的。**

### ⚠️ 兩邊的持倉時間必須一致

| | 參數 | 值 |
|---|---|---|
| GCP | `JN_HOLD_BARS` | 10 根 M15 |
| EA | `InpHoldMinutes` | **150 分鐘** |

GCP **不送 TP 欄位** —— 出場完全交給 EA 的定時。兩邊不一致的話，
持倉會一路抱到 ATR × 8 的災難停損。

### 上線順序（重要）

**先 1，再 5，最後 4。**

1. **先上 main.py。** 新的 EA 靠雲端回 200/403，而 403 的語意變了
   （從「不是趨勢」變成「五關沒全過」）。舊 main.py 配新 EA 不會壞，
   但你會看不到 `failed_checks`。
2. **再上 EA V23。** 換上去之後第一件事：看面板的「獲利平倉門檻」那一格
   應該顯示 **「停用 → 定時出場 150 分鐘」**。若仍顯示金額，代表
   `InpTakeProfitHKD` 沒吃到 0。
3. **最後才換 Pine v4。** 它跟前兩個沒有耦合，隨時可換。

---

## 三、⚠️ 三件你必須手動確認的事

### ⓿ 最危險的一條：EA 的管轄範圍

**V22 完全沒有 magic number 或商品過濾。** `CloseAllPositions()`、
`CheckAndSetSLTP()` 都是直接掃 `PositionsTotal()` —— 帳戶上每一張單都會被動到。

若這個戶口同時有你的手動單或直覺倉，EA 會：
* 幫它們掛上 8000 點（US$80）的止損
* 在持倉滿 150 分鐘時**把它們平掉**

**你的手實測年化 11.67%。那不是 EA 該碰的東西。**

V23 新增三個輸入：

| 輸入 | 預設 | 作用 |
|---|---|---|
| `InpMagicNumber` | `920` | 只管這個 magic 的持倉。**0 = 管全部（危險）** |
| `InpManageSymbolOnly` | `true` | 只管 EA 掛載的那個商品 |
| `InpRiskClosesAll` | `false` | 風控熔斷時是否連非管轄持倉一起平 |

> ⚠️ **前提：你的進場 EA 下單時要設同一個 magic number（920）。**
> 沒設的話 EA 認不得自己的單 → **定時出場不會生效** → 持倉會一路抱到
> 8000 點止損，等於 V22 的失敗模式原封不動回來。
>
> **上線後第一筆單就要驗這一條**：看 Log 有沒有出現
> `⏱️ [定時出場] 單號 ... 持倉 150 分鐘`。沒有就是 magic 沒對上。

### ① EA 的 `ZhugeOrderCheck()` 呼叫端要改

`ZhugeOrderCheck()` 在這個 EA 裡**沒有呼叫點** —— 它是給你的進場 EA 用的閘門。
V23 簽名改成 `ZhugeOrderCheck(bool is_buy = true)`。

有預設值所以**舊呼叫仍能編譯，但只做多不會生效**。呼叫端要改成：

```mql5
if(!ZhugeOrderCheck(true))  return;   // 買單
if(!ZhugeOrderCheck(false)) return;   // 賣單
```

### ② TradingView 的 Properties 分頁會蓋掉腳本

跑 v4 之前逐格確認：

| 欄位 | 值 |
|---|---|
| 初始資金 | 20,000 HKD |
| 預設訂單大小 | 1 數量（= 1 盎司） |
| 佣金 | 2.0 每份合約（→ US$0.40 來回） |
| 做多/空頭槓桿 | 2x（1x 會因保證金不足拒單） |
| 滑點 | 0 ticks |

### ③ 三邊的波動門檻是同一條線，改了要一起改

| 位置 | 參數 | 預設 |
|---|---|---|
| `main.py` | `VOL_FLOOR_ATR_PCT` | 0.10 |
| `zhugeliang_jinnang_v4.pine` | `volFloorPct` | 0.10 |
| 執行單第二題 | 手算 ATR(14)÷現價 | ≥ 0.10% |

量的是同一個東西（ATR(14) ÷ 價格），**打平點實測 0.0837%**。

---

## 四、main.py 的新參數（全部可用環境變數覆寫）

| 參數 | 預設 | 出處 |
|---|---:|---|
| `GATE_DRIVER` | `RISK` | `REGIME` 可一鍵回退成舊行為比對 |
| `LONG_ONLY` | `1` | 全期 671 筆空單每筆 −HK$0.84、t = −0.25 |
| `VOL_FLOOR_ATR_PCT` | `0.10` | 打平點實測 0.0837% |
| `DD_TOLERANCE_PCT` | `20.0` | 空城計可承受回撤 |
| `WORST_GAP_PCT` | `14.0` | 黃金最壞日內 −13.94% |
| `EXPOSURE_HARD_CAP` | `2.0` | 名目 ÷ 淨值 |
| `DAILY_LOSS_LIMIT_PCT` | `3.0` | EA 在 −6% 才強平，兩層分開 |
| `TRAINING_MAX_PER_DAY` | `2` | 以紐約日界線計 |

**五道關卡全過才開閘，缺任何一項數據一律當成不過關（fail-closed）。**

---

## 五、EA 的三個數字改了什麼

| | V22 | V23 |
|---|---:|---:|
| `InpMaxTotalDrawdown` | **90%** | **20%** |
| `InpMaxDailyLossHKD` | 10,000 | 1,200（本金 6%） |
| `InpTakeProfitHKD` | 500 | **0（停用）** |
| 出場 | TP US$64 / SL US$80 | **`InpHoldMinutes = 150`** |
| `InpDefaultSLPoints` | 8000 | 8000（角色改為**災難停損**，正常碰不到） |

TP/SL 那組是幾何必敗：`80 ÷ (64+80) = 55.6%` 才打平，全期 **−HK$20,854**，
平均持倉被拉到 658 分鐘。

---

## 六、分析與文件（不用部署，但要留著）

| 檔案 | 內容 |
|---|---|
| `tradingview/README.md` §22–§26 | 全部量測過程與三次自我更正 |
| `tradingview/sanjigongzhen_sim.py` | 三級共振的離線量測（main.py 邏輯的 Python 移植） |
| `tradingview/jinnang_v4_reconcile.py` | TradingView 對數工具 |
| `tradingview/jinnang_v4_python_baseline.csv/.json` | 已凍結，sha `0987e0cd4a222aaa` |
| `tradingview/JINNANG_V4_PREREG.md` | 對數預登記：預期數字與判準已寫死 |
| `tests/test_risk_gate.py` | 51 項回歸測試 |

---

## 七、這次我自己抓到的三個錯（都已修，記在 README §25）

1. **曝險換算方向反了，低估 60.84 倍**（`equity / rate` 應為 `equity * rate`）
   → 空城計那一關本來永遠會過，等於不存在。
   測試沒抓到是因為只測了上限、沒測曝險本身。現在兩邊都測。
2. **回撤沒對高水位算**（`max(equity, balance)` 不是高水位）→ 改持久化 `equity_peak`。
3. **每次心跳都寫 GCS**（一天 1,440 次）→ 改成真的變動才寫。

另外更正了一個數字：**錦囊 v4 勝率是 53.50%，不是 54.00%** ——
有一筆毛利剛好等於點差、淨損益 0.00，被我算成贏。

---

## 八、沒有改變的一件事

**錦囊的優勢仍未被證實。** 以下全部是 TradingView 全期實測（513 筆，8.46 年）：

| | |
|---|---:|
| 全期年化 | **+2.47%** |
| **樣本外年化**（2018-04 → 2022-08） | **−0.82%** |
| 樣本內年化（2022-08 → 2026-09） | +5.91% |
| 異常值損益 | **佔淨利 106%**（扣掉就是負的） |
| 2026 一年 | 佔全期淨利 **82%** |
| 目前波動水位 | 8.4 年的**第 93 百分位** |

**換錦囊進 GCP 的理由是「它被量過」，不是「它會賺」。**
舊引擎全期去掉最賺 5% 是 −HK$16,945、最大回撤 23.4%，而且從來沒有人量過。

你正站在這條規則八年半以來最有利的環境裡 —— **那是縮手的理由，不是放大的理由。**

**這次所有改動降低的是「輸法」，不是提高「贏面」。**

---

## 九、還沒補的一個缺口

§28 的回測顯示風控五關的最大回撤是 **23.4%**，超過 `DD_TOLERANCE_PCT = 20`。
原因：曝險公式管得住**槓桿**，管不住**連續小額停損的累積**。

**要補一條回撤煞車**（例如回撤到 15% 就停當日新單）。大概 15 行，測試框架已有。
在讓 GCP 碰真錢之前，這一條該補。
